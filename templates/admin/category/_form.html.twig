{{ form_start(form) }}
<div class="row">
    {{ form_row(form.title) }}
    {{ form_row(form.slug) }}


</div>

<button class="btn btn-primary">{{ button|default('Enregistrer') }}</button>

{{ form_end(form) }}